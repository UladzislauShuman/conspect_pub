Тут просто Ссылки и краткие пояснения о чем они 

я не советую читать то, что у меня написано, но источники возможно стоящие


- [Евгений Борисов — Spring-потрошитель, часть 1](https://www.youtube.com/watch?v=BmBr5diz8WA&list=PL6yLoZ_3Y0HKGL3F7vv2SNSrA3TkbXtBX)
    - Spring Bean Lifecycle 
    - BeanDefinitionReader
        - пример interface Quotre и TerminateQuoter
        - определил в XML конфигурации
        - из main вызывал context и getBean
    - получили BeanDefinition-ы
    - BeanFactory -- считывает декларации бинов, кладет в карту
        - там у него IoC Container. в него кладутся Singleton-ы, Prototype сделался и забыли
    - объекты настроены
    - но перед тем как попасть в контейнер вызываются BeanPostProccesor-ы  
        - далее он делал аннотацию InjectRandomInt
        - RetentionPolicy.RUNTIME лучше, так как
            - SOURCE -- не попадет в байткод (@Override)
            - CLASS -- попадет, но рефлексия не читает
            - RUNTIME -- видна в байт коде и рефлексией
        - мы сделаем реализацию BeanPBB
        - конвенции важно
        - есть postProcessBefore и After. кто-то после init метода, кто-то после
            - @PostConstruct как init метод
        - аннотацию с RandomInt запихнули в Before
        - поменял private на public полю и занес
        - Это дало двухфазовый конструктор
            - ибо в контуркторе наше число не настроено
            - только после
    - и BF идет туда, обратно, туда, обратно... в BPP
    - потом init метод Bean
    - и потом Второй раз по этому кругу, чтобы выполнить After
    - а далее о прокси -- бенчмарк сделает, @Profiling
        - DynamicProxy -- имплементация интерфесов. предпочитается Spring
            - влияет на имя. и если по классу хочу полчуить из контекста -- то скорее всего там имя испортилось и все плохо. поэтому стоит брать через интерфейс
        - CJLIB -- наследование. есть ограничения (final классы, как наследовать?)
        - ProfilerController -- флаг содержит. хочет через JMX Consul включать и выключать
            - interface ProdilingControllerMBean, чтобы там было видно. там методы переносим, которые хотим чтобы были видны
            - далее он вставляет, как красиво в AOP
            - можно менять До, После И возвращаемое значение метода
            - надо зарегать в MBeanServer. он решил это сделать в контрукторе
    - далее он стал говорить о Трех Фазовом конструкторе и о ApplicationListener\<Event>
        - про проблему Разогрева Кеша, ибо -- куда ее вставлять, если хочеться воспользоваться аннотациями @Transactional, которые получаются только после postProcessAfterInitialization 
        - этот черт слушает разные ивенты
            - ContextStartedEvent
                - начал построение. не построился
            - ContextStoppedEvent
            - ContextRefreshedEvent
                - после того, как построился, делает refresh
            - ContextClosedEvent
        - о том, как его писали
            - о истории, почему важно не делать грамматические ошибки
            - можно слушать конкретный эвент
            - берет контект, берет имена бинов, хочет пройти по всем бинам и проверить "есть ли у них такая аннотация"
            - по имени класса нельзя на этом этапе -- ибо прокся уже есть
            - @Autowired определенную beanFactory. получит все BeanDefinition и пошла гулянка
                - если вытаскивать не BeanDefinition, то это плохо, ибо -- вдруг класс Lazy, а мы по всем пройдемся -- он создасться, ибо вызвался. и вопрос -- а зачем я тогда это делал Lazy?
            - в Spring инджектить Spring -- это нормально
            - "не буду оставлять пустой catch -- грех!"
            - context -> beanDefinitionNames -> factory -> BeamDif... -> Class -> methods -> isAnnotationPresent
                - если вызывать тут в методе invoke, то -- будет работать только если CJLIB
                - ибо там в примере мы искали в оригинальном классе
                - поэтому мы идем в context, получаем Bean настроенный, получаем его класс (это полученная прокся), из нее метод, по методу До(имя, параметры) и вот уже у него и вызываем
    - итого 
        - Constructor -- Java
        - @PostConstructor -- BPP
        - @AfterProxy -- ContextListener
    
- [Евгений Борисов — Spring-потрошитель, часть 2](https://www.youtube.com/watch?v=cou_qomYLNU&list=PL6yLoZ_3Y0HKGL3F7vv2SNSrA3TkbXtBX&index=3)
    - BeanFactoryPostProcessor
        - настроить BeanDefinition До того, как создаются бины.
        - ну и на BeanFactory тоже
        - пример:
            - подкрутить пароль в класс по "${сюда-пароль}"
        - на примере аннотации @DeprecatedClass
            - тот, кто ставит, обязан указать "кто новый"
            - Т1000 класс сделал от терминатора
            - хочет, чтобы все Бины, которые Устарели, заменились на новые
            - он хочет, чтобы еще До процесса создания бинов BeanFactory6 поменять все классы в BeanDefinition на новую 
            - там он берет все имена бинов, циклом по ним, полчуает definition, там beanClassName
            - по нему получает Класс бина, из него хочет взять аннотацию, и если она есть -- то в его beanDefinition надо установить новое название для класса, которое он возьмет из аннотации, которая сама по себе хранит название класса, На который мы "переходим"
                - но он не искал по всем всем классам, от кого что наследуется
            - история
                - он хотел смегрировать, посмотреть как будет работать система обновленная. поэтому эта штука была удобная
            - "IoC Container --  просто жалкая HashMap"

    - далее об аннотациях и ClassPathBeanDefinitionScanner
        - закалебались в XML
        - примеры
            - @Component
            - @Lazy
            - @DependsOn
            - ...
        - там тоже надо в component-scan задать
        - а можно AnnotationConfigApplicationContext
        - ClassPathBeanDefinitionScanner -- сканирует пакет, ищет бины что аннотированы component и ее наследниками
        - это не BPP, не BFPP, это ResourceLoaderAware
        - потом вышел Java Config (конфигурируется в коде)
            - как доп логику настраивали в XML раньше? -- а там...
            - история про чела, который написал херню на Spell и ушел
            - его читает AnnoratedBeanDefinitionReader
                - ничего не имплементирует
                - часть ApplicationContxet
                - там чето прокси, CJLIB, там делегация на этот @Bean метод...
            - кто их обрабатывает вообще?
                - ConfigurationClassPostProcessor (особый BFPP)
                - он регистрирует как BF с помощью AnnotationConfigApplicationContext
                - создает BD-ы
        - Groovy Config????????

    - Написать свой тип контекста -- PropertyFileApplicationContext
        - exetends GenericApplicationContext
        - конструктор с названием файла
            - PropertyBeanDefinitionReader.loadBeanDefinition
            - чтот-то
            - refresh
        - ничего не вывелось
        - добавили параметр в конфиг
        - потом надо было setter добавить
        - это потом к нему добавляются аннотации и тп
    
    - обновление prototype в Singletone и написание своего scoupe
        - "хочу, чтобы квадратик по экрану летел. и менял цвета"
        - "не вопрос, мы будем использовать Spring"
        - ColorFrame extends JFrame
        - дальше уже...
        - "@ComponentScan("просканиурй там где-то чтобы это")
        - при первом запуске у нас не менялся цвет, ибо вроде Color был Singleton, решил попробовать Prototype
        - он поставил на класс -- теперь у нас куча квадратов запущенных. не удалялись
            - "хорошо понятно, что такое Singleton, что такое Protorype"
        - были варинаты
            - на класс -- куча окон
            - на поле -- один раз создался и сохранилось с Signleton классом
            - на то и другое -- куча окон разного цвета
        - это достойно нового скоупа
        - "а как ты обновляешь Prototype в Singleton?"
            - первое тупое решение -- инджектить context и доставать bean
            - proxyMode = Target_CLASS
                - цена такая,что 
                - каждый раз при обращении к Color -- создается новый объект
            - сделать метод getColor, который сделать абстрактным, как и весь класс... 
                - потом мы его в @Bean, там return new аннонимный класс 
                - и уже из Java Config там мы Лаконично имеем доступ к контексту, и уже далее 
                - там в Java Config был @Bean color, который мы вызвали в нашем новом бине. и который будет возвращать новый бин 
                - это лучше чем предыдущее тем, что я могу сам управлять -- когда мне нужен новый color, а когда нет
                - типо -- могу подсохранить 
        - "хочу, чтобы цвет менялся каждые 3 секунды"
        - другой скоуп -- Pereodical
            - имплементировать интерфейс Scope
            - тут он решил пока только метод get
            - там потом просто -- Spring делегирует в метод get у Scope, где мы уже и сами определим Как выдавать наш бин
            - у нас будет map, где Имя-бина -> (Color, время жизни)
            - далее уже логика реализации get
            - о его регистрации
                - прописать в XML не получится, ибо мне надо где-то сказать, что он отвечает за periodical в аннотации Scope
                - а сказать это нужно контексту
                - надо контексту сказать, что я в тебя зарегаю новый тип scope, за который отвечает вот такой вот объект
                - когда я хочу это сделать?
                - до создания контекста -- BFPP
            - CunstomRegistryBeanFactoryPostProcessor -- наш класс
                - он будет регистрировать любой кастомный scope
                - просто вызывает beanFactory.registerScope(String name, new ClassOfScope)
                - и далее этот BFPP прописать надо как Bean
    
    - о производительности
        - там далее куча сравнений про время. проще в виде посмотреть 1:00:00
        - общие выводы
            - (щас спорно, не шарю) у java есть оптимизация, которая убиарет "бессмысленные" циклы (for (1000000) {создать объект})
            - то есть -- надо хотя бы им как-то воспользовться, чтобы оно не оптимизиоровалось и ничего не сломало
            - но там тоже говно, ибо -- GC, и прочее
            - жрет в инициалиаци. потом норм
            - вызов метода через аспект -- жирно очень. если и делать то для узкоспециализированных методов
                - для обработки ВСЕХ исключений лучше не делать аспект , а сделать свой BPP
                - AspeckJ есть, позже